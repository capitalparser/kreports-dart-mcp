# Copy this to production.env on the server, then replace both values.
# Keep production.env mode 0600 and never commit it.
KREPORTS_MCP_TOKEN=replace-with-a-long-random-token
KREPORTS_BUILD_SHA=replace-with-the-exact-git-commit-sha
