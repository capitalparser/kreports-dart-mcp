# Optional MCP 2026-07-28 sidecar environment.
#
# The default package remains pinned to MCP 1.x for the existing runtime. Build
# the v2 adapter in an isolated environment so pip does not resolve the default
# `mcp<2` dependency:
#
#   python -m venv .venv-mcp-v2
#   . .venv-mcp-v2/bin/activate
#   python -m pip install -e . --no-deps
#   python -m pip install -r requirements-mcp-v2.txt
#
# Windows PowerShell activation:
#   .venv-mcp-v2\Scripts\Activate.ps1

mcp>=2,<3
httpx>=0.27
sqlalchemy>=2.0
typer[all]>=0.13
tabulate>=0.9
pydantic-settings>=2.6
pandas>=2.0
lxml>=5.0
starlette>=0.40
uvicorn[standard]>=0.30
pytest>=8.0
pytest-cov>=5.0
