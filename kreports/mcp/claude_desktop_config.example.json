{
  "mcpServers": {
    "kreports": {
      "command": "uvx",
      "args": ["--from", "kreports", "kreports-mcp"],
      "env": {
        "DB_URL": "sqlite:////absolute/path/to/kreports.db"
      }
    }
  }
}
